#include <Structure/ProjectStructure.hpp>

namespace rus
{
    fs::path GetProjectDir(ProjectDir ProjectDir)
    {
        
    }

    bool GenerateProjectStructure(Project &ec)
    {
        return true;
    }

    bool CheckProjectIntegrity(Project &ec)
    {
        return true;
    }
}